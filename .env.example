# Alchemy API key for Ethereum mainnet (PAYG tier is fine).
# https://dashboard.alchemy.com/
ALCHEMY_API_KEY=

# SQLite location. The file and parent directory will be created on first run.
DATABASE_URL=sqlite://data/unmask.db
